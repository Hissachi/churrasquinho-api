[phases.setup]
nixPkgs = ["php84", "php84Extensions.pdo_mysql"]

[phases.install]
cmds = ["composer install --optimize-autoloader --no-interaction"]

[start]
cmd = "php artisan migrate --force && php -S 0.0.0.0:$PORT -t public"